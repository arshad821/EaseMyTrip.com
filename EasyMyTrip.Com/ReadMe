1.For Parallel Testing Failsafe dependency have been added. but the issue is Webdriver instance 
2. For parallel testing only i have created 2 property file ; so that it will be thread safe.
3. Parallel tetsing is working fine if we use normal wedbriver instead of Webdriver_manager.we have to change all the webdriver refrence.